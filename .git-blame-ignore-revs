# Revisions to ignore in `git blame` (bulk, mechanical, no-behavior-change edits).
# Enable locally with:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
# GitHub honors this file automatically in its blame view.
#
# NOTE: the recorded SHA only takes effect when this branch is integrated with a
# true merge commit (GitHub Desktop's default). A squash- or rebase-merge
# rewrites the SHA and silently voids this entry.

# Apply ruff format (mechanical, no behavior change)
4feefeaf0223604c869c9a64834be0bb4947a6f0
